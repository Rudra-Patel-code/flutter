name: myapp
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0

environment:
    sdk: ^3.8.1

dependencies:
    flutter:
        sdk: flutter
    firebase_core: ^4.0.0
    cloud_firestore: ^6.0.0
    provider: ^6.1.5+1
    table_calendar: ^3.2.0

dev_dependencies:
    flutter_test:
        sdk: flutter
    flutter_lints: ^5.0.0

flutter:
    uses-material-design: true
    assets:
        - assets/

fonts:
    - family: Caveat
      fonts:
          - asset: fonts/Caveat-Regular.ttfP
